package transpiler_lib

ODIN_START_TAG :: "@(Odin_Start)"
ODIN_END_TAG :: "@(Odin_End)"

ORC_OPEN_TAG :: "<Orc = " //Make it so the opening tag must have a name e.g <Orc = dashboard_component>
ORC_CLOSE_TAG :: "</Orc>"
